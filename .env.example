# MySQL Configuration
MYSQL_ROOT_PASSWORD=YourSecureRootPassword2024!
MYSQL_PASSWORD=YourSecureUserPassword2024!

# Redis Configuration  
REDIS_PASSWORD=YourSecureRedisPassword2024!

# Network Configuration
COMPOSE_PROJECT_NAME=docker-services

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Update the passwords with your own secure passwords
# 3. Keep the .env file secure and never commit it to version control
